services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: qonshu
      POSTGRES_PASSWORD: qonshu
      POSTGRES_DB: qonshu
    ports: ["5433:5432"]
    volumes: ["qonshu_pg:/var/lib/postgresql/data"]
volumes:
  qonshu_pg:
